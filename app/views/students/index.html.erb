<h1>All Students</h1>

<%= form_tag(students_path, method: :get) do %>
  <%= text_field_tag :term %>
  <%= submit_tag 'Search' %>
<% end %>

<% @students.each do |s| %>
<%= render partial: 'student', locals: {student: s} %>
<% end %>
